## 14. 監視・可観測性・運用

確認：2026年9月

```choices
rows:
  - role: エラー監視
    default: "**Sentry**"
    tools: [sentry]
  - role: 計装（トレース・メトリクス）
    default: "**OpenTelemetry**"
    tools: [opentelemetry]
  - role: 可観測性基盤（コスト重視）
    default: "**Grafana Cloud**（Loki：ログ、Tempo：トレース、Mimir/Prometheus：メトリクス）"
    tools: [grafana-cloud]
    alts:
      - name: セルフホストのGrafanaスタック
        tools: [grafana]
        when: オンプレ要件
  - role: 可観測性基盤（予算あり・一体型）
    default: "**Datadog**"
    tools: [datadog]
    alts:
      - tools: [new-relic]
        when: 既存の契約や資産があるとき
  - role: AWS標準
    default: "**CloudWatch**（ログの一次保管・アラーム）"
    tools: [cloudwatch]
  - role: 外形監視・ステータスページ
    default: "**Better Stack**"
    tools: [better-stack]
    alts:
      - tools: [uptimerobot]
        when: 無償で最低限
  - role: オンコール・通知
    default: "**PagerDuty**"
    tools: [pagerduty]
    alts:
      - tools: [incident-io]
        when: Slack中心でインシデント対応を回したいとき
    note: 小規模ならSlack通知のみ
  - role: ログ形式
    default: "**構造化ログ（JSON）**"
  - role: 負荷試験
    default: "**k6**"
    tools: [k6]
    alts:
      - tools: [locust]
        when: Pythonでシナリオを書きたいとき
  - role: RUM（実ユーザーの表示速度）
    default: "**Sentry（パフォーマンス機能）**"
    tools: [sentry]
    alts:
      - tools: [vercel-speed-insights]
        when: Vercel採用時
```

### 運用ドキュメント

```choices
head: [役割, 既定]
rows:
  - role: 障害対応手順
    default: "**リポジトリ内 `docs/runbooks/`**"
  - role: ポストモーテム
    default: "**テンプレートを決めて `docs/postmortems/` に蓄積**"
  - role: SLO
    default: "**可用性とレイテンシの2指標から始める**"
```
