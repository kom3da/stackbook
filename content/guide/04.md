## 4. データベース・データ層

確認：2026年9月

### 4-1. DB本体とホスティング

```choices
rows:
  - role: RDB
    default: "**PostgreSQL**"
    tools: [postgresql]
    alts:
      - tools: [mysql]
        when: 既存システムとの互換が必要なときのみ
  - role: 本番ホスティング（AWS）
    default: "**Amazon RDS for PostgreSQL**"
    tools: [amazon-rds-for-postgresql]
    alts:
      - tools: [aurora-postgresql]
        when: 読み取り負荷が高くレプリカを多用する、または高可用性の要件が厳しいとき
  - role: 本番ホスティング（PaaS）
    default: "**Neon**（ブランチ機能でPRごとにDBを複製できる）"
    tools: [neon]
    alts:
      - tools: [supabase]
        when: 認証・ストレージ・リアルタイムも一緒に欲しいとき
    note: NeonはDatabricksの傘下に入った
  - role: 小規模・個人
    default: "**SQLite**（Railsの単一サーバー構成、Cloudflare D1）"
    tools: [sqlite]
  - role: コネクションプール
    default: "**RDS Proxy**（AWS）／ **PgBouncer**（自前）"
    tools: [rds-proxy, pgbouncer]
    note: Neon・Supabaseは組み込みのプーラーを使う
  - role: バックアップ
    default: "**マネージドの自動バックアップ＋ポイントインタイムリカバリ有効化**"
    alts:
      - tools: [pgbackrest]
        when: 自前運用のとき
  - role: バックアップ（SQLite）
    default: "**Litestream**（S3／R2へ継続的にレプリケーション）"
    tools: [litestream]
```

### 4-2. 周辺ツール

```choices
rows:
  - role: GUIクライアント
    default: "**TablePlus**"
    tools: [tableplus]
    alts:
      - tools: [dbeaver]
        when: 無償で多機能なものが必要なとき
  - role: マイグレーションの安全性チェック
    default: "**Squawk**（ロックを取る危険なALTERを検出）"
    tools: [squawk]
  - role: スキーマ可視化
    default: "**tbls**（スキーマからドキュメントとER図を生成）"
    tools: [tbls]
  - role: テストデータ
    default: 各言語のファクトリ／シード
  - role: 本番データの匿名化
    default: "**pg_dump後にスクリプトで置換**"
    tools: [pg-dump]
    alts:
      - tools: [postgresql-anonymizer]
        when: 頻繁に必要なとき
```

### 4-3. Postgresで済ませる範囲

| 用途 | Postgresでの手段 | 専用ツールへの乗り換え条件 |
|---|---|---|
| ジョブキュー | River（Go）、Solid Queue（Rails）、pg-boss（TS） | §23-1 の目安を超える（Goは秒間数千件、Rails・TSは秒間数百件） → Redis系／SQS |
| 全文検索（日本語） | **pg_bigm**（2文字単位の部分一致、日本語に強い） | 表記揺れ・ファセット・高速サジェストが必要 → Meilisearch |
| ベクトル検索 | **pgvector** | 数千万件以上・専用のフィルタリング要件 → Qdrant |
| キャッシュ | Solid Cache（Rails） | 低レイテンシ必須 → Valkey/Redis |
| Pub/Sub | LISTEN/NOTIFY | 大量配信 → 専用基盤 |

### 4-4. キャッシュ・キュー・ストリーム

```choices
rows:
  - role: インメモリKVS
    default: "**Valkey**（Redis互換のOSS。AWSではElastiCache for Valkey）"
    tools: [valkey]
    alts:
      - tools: [redis]
        when: 既存資産
  - role: マネージドキュー（AWS）
    default: "**SQS**"
    tools: [sqs]
  - role: イベント配信（AWS）
    default: "**EventBridge**"
    tools: [eventbridge]
    alts:
      - tools: [sns]
        when: 単純なファンアウトだけのとき
  - role: ストリーム
    default: 基本的に不要
    alts:
      - name: Kafka（MSK）／Redpanda
        tools: [kafka, amazon-msk, redpanda]
        when: 大量のイベントを複数サービスで再処理する要件があるとき
  - role: ワークフロー
    default: "**Temporal**（長時間・複雑なもの）"
    tools: [temporal]
    alts:
      - tools: [inngest]
        when: サーバーレス環境
      - tools: [aws-step-functions]
        when: AWS内で完結させたいとき
```
