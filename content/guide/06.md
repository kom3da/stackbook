## 6. 認証・認可

確認：2026年9月

```choices
head: [ケース, 既定, 代替と乗り換え条件]
rows:
  - role: TSアプリ（自前で持つ）
    default: "**Better Auth**"
    tools: [better-auth]
    alts:
      - tools: [auth-js]
        when: 既存プロジェクトのみ（Auth.jsの保守はBetter Authに移管された）
    note: Better AuthはVercelの傘下に入った（OSSでフレームワーク非依存の方針は維持）
  - role: マネージド（toC・スタートアップ）
    default: "**Clerk**（UI込み・最短で導入）"
    tools: [clerk]
    alts:
      - tools: [supabase-auth]
        when: Supabase採用時
      - tools: [firebase-authentication]
        when: モバイル中心
  - role: マネージド（AWS中心）
    default: "**Amazon Cognito**"
    tools: [amazon-cognito]
    alts:
      - tools: [auth0]
        when: 機能とUIを重視し予算があるとき
  - role: B2BのSSO（SAML／OIDC）・SCIM
    default: "**WorkOS**"
    tools: [workos]
    alts:
      - name: Auth0（Enterprise機能）
        tools: [auth0]
        when: 取引先の要件でAuth0が指定されている、またはIdP連携の種類がWorkOSで足りないとき
  - role: セルフホストIdP
    default: "**Keycloak**"
    tools: [keycloak]
    alts:
      - tools: [zitadel]
        when: 軽量・モダンなものが欲しいとき
  - role: Rails
    default: "**Railsの認証ジェネレータ**（§2-3参照）"
    tools: [rails]
    alts:
      - tools: [devise]
        when: OAuth連携・多要素認証など機能を一式揃えたいとき（§2-3）
  - role: Go
    default: "**外部IdPに任せ、アプリはOIDCトークンの検証のみ**（coreos/go-oidc）"
    tools: [idp]
  - role: 社内ツール
    default: "**Google Workspace／Microsoft Entra IDでのSSO**"
    tools: [google-workspace, microsoft-entra-id]
  - role: 認可ロジック（複雑な権限）
    default: "**アプリ内でポリシーを明示的にコード化**（Pundit、自前）"
    alts:
      - name: OpenFGA／SpiceDB
        tools: [openfga, spicedb]
        when: リソース単位の共有・継承が複雑なとき（Googleドライブ型）
  - role: パスワード以外
    default: "**パスキー（WebAuthn）対応を優先**"
```
