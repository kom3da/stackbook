## 2. 言語の決め方と言語別の既定

確認：2026年9月

### 2-1. TypeScript（Node.js）

```choices
rows:
  - role: ランタイム
    default: "**Node.js（LTS）**"
    tools: [node-js]
    alts:
      - tools: [bun]
        when: スクリプト・CLI・新規の小規模APIで速度を重視するとき
      - tools: [deno]
        when: Deno Deploy前提のとき
    note: BunはAnthropicの傘下に入った（ライセンスはMITのまま）
  - role: パッケージマネージャ
    default: "**pnpm**"
    tools: [pnpm]
    alts:
      - tools: [bun]
        when: ランタイムもBunにする場合
  - role: モノレポ
    default: "**pnpm workspaces ＋ Turborepo**"
    tools: [pnpm-workspaces, turborepo]
    alts:
      - tools: [nx]
        when: 大規模でコード生成や依存グラフの可視化が必要なとき
  - role: Lint＋フォーマット
    default: "**Biome**（1ツールで完結・高速）"
    tools: [biome]
    alts:
      - name: ESLint（flat config）＋typescript-eslint＋Prettier
        tools: [eslint, typescript-eslint, prettier]
        when: Biomeが未対応のルールやプラグイン（Next.js固有ルールなど）が必須のとき
  - role: 型チェック
    default: "**tsc --noEmit**（`strict: true`）"
    tools: [tsc-noemit]
  - role: テスト（ユニット）
    default: "**Vitest**"
    tools: [vitest]
    alts:
      - tools: [jest]
        when: 既存プロジェクトのみ
  - role: テスト（E2E）
    default: "**Playwright**"
    tools: [playwright]
  - role: APIモック
    default: "**MSW**"
    tools: [msw]
  - role: バリデーション
    default: "**Zod**"
    tools: [zod]
    alts:
      - tools: [valibot]
        when: バンドルサイズを極限まで削りたいフロント
  - role: HTTPフレームワーク
    default: "**Hono**（軽量・どこでも動く・型安全なRPC）"
    tools: [hono]
    alts:
      - tools: [nestjs]
        when: 10人以上のチームでDI・モジュール構造を強制したいとき
      - tools: [fastify]
        when: Node専用で既存プラグイン資産を使いたいとき
  - role: OpenAPI
    default: "**@hono/zod-openapi**（Zodスキーマからルートと仕様を同時に定義）"
    tools: [hono-zod-openapi]
  - role: ORM／クエリ
    default: "**Drizzle**（SQLに近い・軽い・エッジ対応）"
    tools: [drizzle]
    alts:
      - tools: [prisma]
        when: スキーマ駆動でGUI（Studio）やマイグレーションの手厚さを重視するとき
      - tools: [kysely]
        when: ORMを使わず型付きクエリビルダーだけ欲しいとき
  - role: マイグレーション
    default: "**drizzle-kit**"
    tools: [drizzle-kit]
    alts:
      - tools: [prisma-migrate]
        when: ORMにPrismaを採用したとき
  - role: ジョブキュー
    default: "**pg-boss**（Postgresベース、§4-3）"
    tools: [pg-boss]
    alts:
      - name: BullMQ（Redis/Valkey）
        tools: [bullmq]
        when: 秒間数百件を超える、またはValkeyが既にあるとき（§23-1）
      - name: Inngest／Trigger.dev
        tools: [inngest, trigger-dev]
        when: サーバーレス環境でワークフロー（リトライ・待機・ステップ実行）が必要なとき
  - role: ロギング
    default: "**pino**"
    tools: [pino]
  - role: 設定・環境変数の検証
    default: "**Zodでスキーマ定義して起動時に検証**（t3-envなど）"
    tools: [zod]
  - role: 日付
    default: "**date-fns**"
    tools: [date-fns]
    alts:
      - tools: [temporal-api]
        when: 対応環境が揃っていればネイティブを使う
      - tools: [day-js]
        when: 軽さ最優先
  - role: HTTPクライアント
    default: "**fetch（標準）**"
    tools: [fetch]
    alts:
      - tools: [ky]
        when: リトライやフックを簡潔に書きたいとき
  - role: ビルド（ライブラリ）
    default: "**tsdown**"
    tools: [tsdown]
    alts:
      - tools: [tsup]
        when: 既存プロジェクトのみ（tsupは保守されていないため、機会があれば移行する）
  - role: 実行（開発時）
    default: "**Node.jsの型除去による直接実行**（`node --watch`）"
    tools: [node-js]
    alts:
      - tools: [tsx]
        when: enum・デコレータなど型除去で消せない構文や、パスエイリアスを使うとき
  - role: 脆弱性チェック
    default: "**pnpm audit ＋ Renovate/Dependabot**"
    tools: [pnpm-audit, renovate, dependabot]
```

### 2-2. Go

```choices
rows:
  - role: 依存管理
    default: "**Go modules**"
    tools: [go-modules]
  - role: Lint
    default: "**golangci-lint**（設定ファイルで有効ルールを明示）"
    tools: [golangci-lint]
  - role: フォーマット
    default: "**gofumpt**（gofmtより厳格）"
    tools: [gofumpt]
    alts:
      - tools: [gofmt]
        when: チームに合わせる場合
  - role: HTTPルーティング
    default: "**標準 net/http**（メソッド・パスパラメータ対応済み）＋ ミドルウェアは自前か小さなライブラリ"
    tools: [net-http]
    alts:
      - tools: [chi]
        when: ミドルウェアのエコシステムを使いたいとき
      - tools: [echo]
        when: フレームワーク一式を求めるチーム
  - role: DBドライバ
    default: "**pgx**"
    tools: [pgx]
  - role: クエリ
    default: "**sqlc**（SQLから型安全なコードを生成）"
    tools: [sqlc]
    alts:
      - tools: [ent]
        when: グラフ構造のスキーマが中心のとき
    note: GORMは暗黙の挙動が多く、性能問題の調査が難しいため選ばない（§20）
  - role: マイグレーション
    default: "**goose**"
    tools: [goose]
    alts:
      - tools: [atlas]
        when: 宣言的スキーマ管理（あるべき状態から差分生成）をしたいとき
      - tools: [golang-migrate]
        when: 既存プロジェクト
  - role: OpenAPI
    default: "**oapi-codegen**（スキーマファースト）"
    tools: [oapi-codegen]
    alts:
      - tools: [ogen]
        when: 生成コードの性能・厳密さを重視するとき
  - role: RPC
    default: "**Connect（connect-go）**"
    tools: [connect]
    alts:
      - tools: [grpc]
        when: 既存のgRPC基盤がある場合
  - role: ジョブキュー
    default: "**River**（Postgresベース、トランザクション内でジョブ投入可能）"
    tools: [river]
    alts:
      - tools: [asynq]
        when: Redis前提のとき
  - role: ロギング
    default: "**log/slog（標準）**"
    tools: [log-slog]
  - role: 設定
    default: "**環境変数＋envconfig（caarlos0/env など）**"
    tools: [envconfig]
    alts:
      - tools: [koanf]
        when: 複数ソース（ファイル・環境変数・リモート）を統合したいとき
  - role: CLI
    default: "**Cobra**"
    tools: [cobra]
    alts:
      - tools: [urfave-cli]
        when: 小規模CLI
  - role: テスト
    default: "**標準 testing ＋ go-cmp**"
    tools: [testing, go-cmp]
    alts:
      - tools: [testify]
        when: アサーションを簡潔にしたいチーム
  - role: 統合テスト
    default: "**testcontainers-go**（実DBでテスト）"
    tools: [testcontainers-go]
  - role: モック
    default: "**インターフェース＋手書きフェイク**"
    alts:
      - name: mockery／gomock
        tools: [mockery, gomock]
        when: モック対象が多いとき
  - role: ホットリロード
    default: "**air**"
    tools: [air]
  - role: 脆弱性チェック
    default: "**govulncheck**"
    tools: [govulncheck]
  - role: リリース
    default: "**GoReleaser**"
    tools: [goreleaser]
```

### 2-3. Ruby / Rails

Railsの標準構成（「Rails omakase」）にできるだけ乗る。外部依存を増やさないことが最大のメリットになる。

```choices
rows:
  - role: バージョン管理
    default: "**mise**"
    tools: [mise]
    alts:
      - tools: [rbenv]
        when: 既存環境
  - role: 依存管理
    default: "**Bundler**"
    tools: [bundler]
  - role: Lint＋フォーマット
    default: "**RuboCop（rubocop-rails-omakase）**"
    tools: [rubocop]
    alts:
      - tools: [standard]
        when: 設定を一切考えたくないとき
  - role: テスト
    default: "**Minitest（Rails標準）＋ fixtures**"
    tools: [minitest, fixtures]
    alts:
      - name: RSpec＋FactoryBot
        tools: [rspec, factorybot]
        when: チームがRSpecに慣れている、または既存資産があるとき
  - role: システムテスト
    default: "**Capybara＋Selenium（Rails標準）**"
    tools: [capybara, selenium]
    alts:
      - name: Playwright（capybara-playwright-driver）
        tools: [playwright, capybara-playwright-driver]
        when: 安定性・速度を重視するとき
  - role: フロント
    default: "**Hotwire（Turbo＋Stimulus）＋ importmap**"
    tools: [hotwire, importmap]
    alts:
      - name: jsbundling＋esbuild
        tools: [jsbundling, esbuild]
        when: npmパッケージを多く使うとき
  - role: CSS
    default: "**tailwindcss-rails**"
    tools: [tailwindcss-rails]
  - role: アセット
    default: "**Propshaft（Rails標準）**"
    tools: [propshaft]
  - role: コンポーネント
    default: "**ViewComponent**"
    tools: [viewcomponent]
    alts:
      - tools: [phlex]
        when: Rubyだけでビューを書きたいとき
  - role: ジョブ
    default: "**Solid Queue**（DBベース）"
    tools: [solid-queue]
    alts:
      - tools: [sidekiq]
        when: 非常に高スループットが必要なとき
  - role: キャッシュ
    default: "**Solid Cache**"
    tools: [solid-cache]
    alts:
      - tools: [redis]
        when: 既存のRedis基盤がある場合
  - role: WebSocket
    default: "**Action Cable＋Solid Cable**"
    tools: [action-cable, solid-cable]
    alts:
      - tools: [anycable]
        when: 同時接続数が非常に多いとき
  - role: 認証
    default: "**Railsの認証ジェネレータ**"
    tools: [rails]
    alts:
      - tools: [devise]
        when: OAuth連携・多要素認証など機能を一式揃えたいとき
      - tools: [rodauth]
        when: 高度なセキュリティ要件
  - role: 認可
    default: "**Pundit**"
    tools: [pundit]
    alts:
      - tools: [action-policy]
        when: ルールが複雑でキャッシュやテスト支援が欲しいとき
  - role: 管理画面
    default: "**Avo**"
    tools: [avo]
    alts:
      - tools: [administrate]
        when: 無償・シンプルで足りるとき
  - role: ページネーション
    default: "**Pagy**"
    tools: [pagy]
  - role: ファイル
    default: "**Active Storage**"
    tools: [active-storage]
  - role: デプロイ
    default: "**Kamal**"
    tools: [kamal]
    alts:
      - name: Render／Fly.io
        tools: [render, fly-io]
        when: サーバー管理をしたくないとき
  - role: セキュリティ静的解析
    default: "**Brakeman**"
    tools: [brakeman]
  - role: 依存の脆弱性
    default: "**bundler-audit**"
    tools: [bundler-audit]
  - role: N+1検出
    default: "**Prosopite**"
    tools: [prosopite]
    alts:
      - tools: [bullet]
        when: 開発中にブラウザ上の通知でN+1を知りたいとき
  - role: モバイル
    default: "**Hotwire Native**"
    tools: [hotwire-native]
    alts:
      - tools: [react-native]
        when: ネイティブ体験を重視するとき
```

### 2-4. Python（AI/ML、データ処理が必要な場合に限定）

```choices
rows:
  - role: バージョン・依存管理
    default: "**uv**（Python本体の導入からロックファイルまで1本）"
    tools: [uv]
    alts:
      - tools: [poetry]
        when: 既存プロジェクト
  - role: Lint＋フォーマット
    default: "**Ruff**"
    tools: [ruff]
  - role: 型チェック
    default: "**Pyright**"
    tools: [pyright]
    alts:
      - name: mypy
        when: 既存プロジェクトが使っているとき
  - role: テスト
    default: "**pytest**"
    tools: [pytest]
  - role: Webフレームワーク
    default: "**FastAPI**"
    tools: [fastapi]
    alts:
      - tools: [django]
        when: 管理画面付きのCRUDアプリをPythonで作る必要があるとき
  - role: バリデーション
    default: "**Pydantic**"
    tools: [pydantic]
  - role: ORM
    default: "**SQLAlchemy（2.x形式）**"
    tools: [sqlalchemy]
    alts:
      - tools: [sqlmodel]
        when: FastAPIと密に統合したい小規模案件
  - role: マイグレーション
    default: "**Alembic**"
    tools: [alembic]
  - role: HTTPクライアント
    default: "**httpx**"
    tools: [httpx]
  - role: ジョブ
    default: "**Celery**"
    tools: [celery]
    alts:
      - name: arq／Dramatiq
        tools: [arq, dramatiq]
        when: 軽量に済ませたいとき
      - tools: [temporal]
        when: 長時間ワークフロー
  - role: ロギング
    default: "**structlog**"
    tools: [structlog]
  - role: データ処理
    default: "**Polars**"
    tools: [polars]
    alts:
      - tools: [pandas]
        when: ライブラリ連携上必要なとき
  - role: ノートブック
    default: "**Jupyter**"
    tools: [jupyter]
    alts:
      - tools: [marimo]
        when: Gitで差分管理しやすいノートブックが欲しいとき
```

### 2-5. Rust

```choices
rows:
  - role: ビルド・依存
    default: "**Cargo**"
    tools: [cargo]
  - role: Lint／フォーマット
    default: "**clippy ／ rustfmt**"
    tools: [clippy, rustfmt]
  - role: 非同期ランタイム
    default: "**Tokio**"
    tools: [tokio]
  - role: Webフレームワーク
    default: "**axum**"
    tools: [axum]
    alts:
      - tools: [actix-web]
        when: 既存資産がある場合
  - role: DB
    default: "**sqlx**"
    tools: [sqlx]
    alts:
      - tools: [seaorm]
        when: ORMが欲しいとき
  - role: シリアライズ
    default: "**serde**"
    tools: [serde]
  - role: ロギング／トレース
    default: "**tracing**"
    tools: [tracing]
  - role: CLI
    default: "**clap**"
    tools: [clap]
  - role: エラー
    default: "**thiserror（ライブラリ）／ anyhow（アプリ）**"
    tools: [thiserror, anyhow]
  - role: テスト実行
    default: "**cargo-nextest**"
    tools: [cargo-nextest]
  - role: ベンチマーク
    default: "**criterion**"
    tools: [criterion]
    alts:
      - tools: [divan]
        when: 記述を簡潔にしたいとき
  - role: 脆弱性チェック
    default: "**cargo-audit ／ cargo-deny**"
    tools: [cargo-audit, cargo-deny]
  - role: バイナリ配布
    default: "**cargo-dist**"
    tools: [cargo-dist]
    alts:
      - name: GoReleaser（Rust対応あり）
        tools: [goreleaser, rust]
        when: Goと配布の仕組みを揃えたいとき
  - role: クロスコンパイル
    default: "**cargo-zigbuild**"
    tools: [cargo-zigbuild]
    alts:
      - tools: [cross]
        when: Dockerで環境ごと切り替えたいとき
  - role: WebAssembly
    default: "**wasm-bindgen＋wasm-pack**"
    tools: [wasm-bindgen, wasm-pack]
  - role: Node.jsから呼ぶネイティブ拡張
    default: "**napi-rs**"
    tools: [napi-rs]
  - role: Pythonから呼ぶネイティブ拡張
    default: "**PyO3＋maturin**"
    tools: [pyo3, maturin]
  - role: デスクトップアプリ
    default: "**Tauri**（UIはWeb技術、裏側はRust）"
    tools: [tauri]
    alts:
      - tools: [electron]
        when: Node.jsのAPIやChromiumの挙動に強く依存するとき
  - role: 組み込み（非同期）
    default: "**Embassy**"
    tools: [embassy]
    alts:
      - tools: [rtic]
        when: 割り込み駆動で厳密なリアルタイム性が必要なとき
  - role: gRPC
    default: "**tonic**"
    tools: [tonic]
```

### 2-6. Kotlin（JVM）

既存のJava資産との連携、複雑な業務ルール、大規模バッチ、Kafkaなどのストリーム処理で選ぶ。

```choices
rows:
  - role: JDK
    default: "**Eclipse Temurin（LTS）をmiseで管理**"
    tools: [eclipse-temurin]
    alts:
      - tools: [amazon-corretto]
        when: AWS上での実行に揃えたいとき
  - role: ビルド
    default: "**Gradle（Kotlin DSL）**"
    tools: [gradle]
    alts:
      - tools: [maven]
        when: 既存プロジェクト
  - role: Lint／フォーマット
    default: "**ktlint ＋ detekt**"
    tools: [ktlint, detekt]
  - role: Webフレームワーク
    default: "**Spring Boot**"
    tools: [spring-boot]
    alts:
      - tools: [ktor]
        when: 軽量に作りたい、Springの規模が過剰なとき
  - role: DBアクセス
    default: "**jOOQ**（SQLに近い型安全なクエリ）"
    tools: [jooq]
    alts:
      - tools: [spring-data-jpa]
        when: 単純なCRUDが大半のとき
      - tools: [exposed]
        when: Kotlinらしく書きたい小〜中規模
  - role: マイグレーション
    default: "**Flyway**"
    tools: [flyway]
    alts:
      - tools: [liquibase]
        when: DBの種類を横断して管理したいとき
  - role: 非同期
    default: "**Kotlinコルーチン**"
    tools: [kotlin]
  - role: シリアライズ
    default: "**Jackson（Spring標準）**"
    tools: [jackson]
    alts:
      - tools: [kotlinx-serialization]
        when: Ktor採用時
  - role: テスト
    default: "**JUnit ＋ Kotest（アサーション）＋ MockK**"
    tools: [junit, kotest, mockk]
  - role: 統合テスト
    default: "**Testcontainers**"
    tools: [testcontainers]
  - role: バッチ
    default: "**Spring Batch**"
    tools: [spring-batch]
  - role: ストリーム処理
    default: "**Spring for Apache Kafka**"
    tools: [spring-for-apache-kafka]
    alts:
      - tools: [kafka-streams]
        when: 状態を持つ集計処理
  - role: OpenAPI
    default: "**springdoc-openapi**"
    tools: [springdoc-openapi]
  - role: ロギング
    default: "**SLF4J＋Logback（JSON出力）**"
    tools: [slf4j, logback]
  - role: コンテナ化
    default: "**Jib**（Dockerfileなしでイメージ作成）"
    tools: [jib]
    alts:
      - tools: [dockerfile]
        when: 細かく制御したいとき
  - role: 起動速度の改善
    default: 通常は不要
    alts:
      - name: GraalVMネイティブイメージ
        tools: [graalvm]
        when: サーバーレスで起動時間が問題になるとき
```

### 2-7. Elixir（BEAM）

大量の常時接続、プレゼンス（誰がオンラインか）、障害からの自動復旧が中心の常駐サービスで選ぶ。

```choices
rows:
  - role: バージョン管理
    default: "**mise（Erlang／Elixir）**"
    tools: [mise]
  - role: ビルド・依存
    default: "**Mix ＋ Hex**"
    tools: [mix, hex]
  - role: フォーマット
    default: "**mix format**"
    tools: [mix-format]
  - role: Lint
    default: "**Credo**"
    tools: [credo]
  - role: 型チェック
    default: "**Dialyxir**（Elixir本体の型システムも段階的に強化中）"
    tools: [dialyxir]
  - role: Webフレームワーク
    default: "**Phoenix**"
    tools: [phoenix]
    alts:
      - name: Plug単体
        tools: [plug]
        when: APIだけの極小サービス
  - role: 画面
    default: "**Phoenix LiveView**（サーバー主導でリアルタイムUI）"
    tools: [phoenix-liveview]
    alts:
      - name: Next.js＋Phoenix Channels
        tools: [next-js, phoenix-channels]
        when: フロントをReactで作りたいとき
  - role: リアルタイム
    default: "**Phoenix Channels ＋ Phoenix Presence**"
    tools: [phoenix-channels, phoenix-presence]
  - role: DB
    default: "**Ecto**（マイグレーション含む）"
    tools: [ecto]
  - role: ジョブ
    default: "**Oban**（Postgresベース）"
    tools: [oban]
  - role: データパイプライン
    default: "**Broadway**（SQS・Kafka等からの消費）"
    tools: [broadway]
  - role: HTTPクライアント
    default: "**Req**"
    tools: [req]
  - role: テスト
    default: "**ExUnit ＋ Mox**"
    tools: [exunit, mox]
  - role: セキュリティ静的解析
    default: "**Sobelow**"
    tools: [sobelow]
  - role: 計装
    default: "**Telemetry ＋ OpenTelemetry**"
    tools: [telemetry, opentelemetry]
  - role: デプロイ
    default: "**mix release をDockerイメージ化** → ECS／Fly.io／Kamal"
    tools: [mix-release]
```

### 2-8. C#（.NET）

Unityのクライアントとサーバーでコードを共有したいとき、Microsoft／Azure中心の環境、Windows前提の業務システムで選ぶ。

```choices
rows:
  - role: SDK
    default: "**.NET（LTS）をmiseで管理**"
    tools: [net]
  - role: ビルド・依存
    default: "**dotnet CLI ＋ NuGet**"
    tools: [dotnet-cli, nuget]
  - role: フォーマット／Lint
    default: "**dotnet format ＋ .NETアナライザー**（.editorconfigで設定）"
    tools: [dotnet-format, net]
  - role: Webフレームワーク
    default: "**ASP.NET Core（Minimal API）**"
    tools: [asp-net-core]
    alts:
      - name: コントローラー方式
        when: 大規模で構造を揃えたいとき
  - role: DBアクセス
    default: "**EF Core**"
    tools: [ef-core]
    alts:
      - tools: [dapper]
        when: SQLを直接書いて性能を詰めたいとき
  - role: マイグレーション
    default: "**EF Core Migrations**"
    tools: [ef-core-migrations]
  - role: リアルタイム
    default: "**SignalR**"
    tools: [signalr]
  - role: Unityとの通信
    default: "**MagicOnion**（gRPCベースでC#の型をクライアントと共有）"
    tools: [magiconion]
  - role: ジョブ・スケジュール
    default: "**Quartz.NET**"
    tools: [quartz-net]
  - role: テスト
    default: "**xUnit ＋ NSubstitute**"
    tools: [xunit, nsubstitute]
  - role: ロギング
    default: "**Serilog（構造化ログ）**"
    tools: [serilog]
  - role: OpenAPI
    default: "**ASP.NET Core標準のOpenAPI生成 ＋ Scalar**"
    tools: [asp-net-core, scalar]
  - role: コンテナ化
    default: "**dotnet publish のコンテナ出力**"
    tools: [dotnet-publish]
    alts:
      - tools: [dockerfile]
        when: OSパッケージの追加などビルドを細かく制御したいとき
```

### 2-9. 作成物ごとの言語の決め方

言語は「何を作るか」で決まる。下表で当てはまる行を探して既定を使い、右の列の条件に当てはまるときだけ乗り換える。バックエンドはさらに細かく §2-10 で判断する。

```choices
head: [作成物, 既定, 別の選択肢と乗り換える条件]
rows:
  - role: CRUD中心のWebアプリ・管理画面
    default: "**Rails**（画面の多い業務システムを最も速く作れる）"
    tools: [rails]
    alts:
      - tools: [typescript]
        when: 外部API連携が中心、またはReactで画面を作り込みたいとき
      - tools: [kotlin]
        when: 業務ルールが非常に複雑で、既存のJava資産と連携するとき
  - role: 一般的なWeb API・SaaSのバックエンド
    default: "**TypeScript**（フロントと1言語に揃う）"
    tools: [typescript]
    alts:
      - tools: [go]
        when: 3人以上で公開APIを長期運用するとき（§2-10 手順3）
    note: 負荷の性質で決まる場合は §2-10 を優先する
  - role: CLIツール
    default: "**Go**"
    tools: [go]
    alts:
      - tools: [rust]
        when: 起動時間・バイナリサイズ・処理速度を詰めたい
      - name: TypeScript（Bun）
        tools: [typescript, bun]
        when: 社内向けでnpmの資産を使いたい
  - role: インフラ系ツール（Kubernetes Operator、Terraformプロバイダ等）
    default: "**Go**（公式SDKがGo中心）"
    tools: [go]
  - role: 開発者向けツール（Linter、フォーマッタ、ビルドツール、パーサ）
    default: "**Rust**"
    tools: [rust]
    alts:
      - tools: [go]
        when: 処理が軽く、開発速度を優先したいとき
  - role: ブラウザ内の重い処理（画像・動画処理、暗号、大量データの解析）
    default: "**Rust → WebAssembly**"
    tools: [rust, webassembly]
    alts:
      - name: TypeScript＋Web Worker
        tools: [typescript, web-worker]
        when: 処理がそこまで重くないとき
  - role: エッジ関数
    default: "**TypeScript**（Cloudflare Workers）"
    tools: [typescript]
    alts:
      - name: Rust→WebAssembly
        tools: [rust, webassembly]
        when: CPU負荷の高い処理がある
  - role: 他言語から呼び出すライブラリ
    default: "**Rust**（napi-rs、PyO3など）"
    tools: [rust]
  - role: デスクトップアプリ
    default: "**Tauri**"
    tools: [tauri]
    alts:
      - tools: [electron]
        when: Node.jsのAPIに強く依存するとき
      - name: C#（.NET）
        tools: [csharp, net]
        when: Windows専用の業務アプリ
  - role: モバイルアプリ
    default: "**React Native＋Expo**"
    tools: [react-native, expo]
    alts:
      - name: Kotlin／Swift（ネイティブ）
        tools: [kotlin, swift]
        when: 端末機能を深く使うとき
      - tools: [flutter]
        when: デザインの一貫性を最優先するとき
  - role: ゲームのクライアント
    default: エンジンに従う（Unity：C#、Unreal：C++）
  - role: 組み込み・IoT・ファームウェア
    default: "**Rust**"
    tools: [rust]
    alts:
      - name: C
        when: ベンダーSDKがCしかないとき
  - role: データベース・検索エンジン等の基盤ソフト
    default: "**Rust**"
    tools: [rust]
    alts:
      - tools: [go]
        when: GCの影響が許容できる分散系の制御部分
  - role: データ分析・集計
    default: "**SQL**（DuckDB、BigQuery、Postgres）"
    tools: [sql]
    alts:
      - name: Python（Polars）
        tools: [python, polars]
        when: SQLで表現しにくい変換があるとき
  - role: 機械学習の学習・推論
    default: "**Python**"
    tools: [python]
    alts:
      - name: Rust（candle等）
        tools: [rust, candle]
        when: 小さなモデルを単一バイナリで配りたいとき
  - role: LLM API呼び出し中心のAIアプリ
    default: "**TypeScript**"
    tools: [typescript]
    alts:
      - tools: [python]
        when: 埋め込み処理や評価などML系ライブラリを本格的に使うとき
  - role: スクリプト・社内の小さな自動化
    default: "**TypeScript**（Node.jsで直接実行、またはBun）"
    tools: [typescript]
    alts:
      - tools: [go]
        when: 単一バイナリで配りたいとき
      - name: シェルスクリプト
        when: 数行で済むとき
```

### 2-10. バックエンドの言語の決め方

バックエンドは「どんな負荷がかかるか」と「何に依存するか」で最適な言語が変わる。以下の順に確認し、最初に当てはまったところで決める。

#### 手順1：外部の制約で決まるか

実行環境の制約、コードを共有する相手の制約、組織の制約、ライブラリの制約の順に確認する（実行できない言語は、ライブラリがあっても選べないため）。

```uses
head: [制約, 選ぶ言語]
rows:
  - situation: 実行環境がエッジ（Cloudflare Workers等）
    pick: "**TypeScript**"
    tools: [typescript]
  - situation: Unityのクライアントとモデルや通信定義を共有したい
    pick: "**C#**"
    tools: [csharp]
  - situation: 既存のJava資産（社内基盤、外部の業務パッケージ）と密に連携する
    pick: "**Kotlin**"
    tools: [kotlin]
  - situation: Microsoft／Azure中心で、運用チームが.NETに慣れている
    pick: "**C#**"
    tools: [csharp]
  - situation: 必須のライブラリ・SDKが特定の言語にしかない（機械学習、特定ベンダーのSDK等）
    pick: その言語（機械学習ならPython）
  - situation: Kubernetes・クラウドのコントロールプレーンを扱う
    pick: "**Go**"
    tools: [go]
```

#### 手順2：負荷の性質で決める

```uses
head: [負荷の性質, 第一候補, 次点, 典型例, 理由]
rows:
  - situation: DBの読み書きと業務ロジックが中心
    pick: "**TypeScript**（画面の多い業務システムなら **Rails**）"
    tools: [typescript, rails]
    next: Go（3人以上で公開APIを長期運用するとき）
    example: 管理画面、予約、受発注、SaaSの大半
    reason: ボトルネックはDB。言語の性能差はほぼ効かないので、開発速度とチームで選ぶ
  - situation: 外部APIの待ち時間が中心
    pick: "**TypeScript**"
    tools: [typescript]
    next: Go
    example: BFF、複数APIの集約、Webhook中継
    reason: 非同期I/Oの書きやすさと、フロントとの型共有
  - situation: 大量の常時接続（数万接続以上が目安、§23-3）
    pick: "**Elixir**"
    tools: [elixir]
    next: Go
    example: チャット、プレゼンス、通知配信、共同編集
    reason: 軽量プロセスで数十万規模の接続を扱え、落ちた処理を自動で再起動する仕組みが言語に組み込まれている。それ未満のチャットや通知はマネージド（§19-9）で足りる
  - situation: 中程度の常時接続＋高スループット
    pick: "**Go**"
    tools: [go]
    next: Rust
    example: APIゲートウェイ、プロキシ、ゲームのロビー
    reason: goroutineで並行処理が簡潔に書け、運用も単純
  - situation: レイテンシの上振れが許容できない（p99で10ms未満が厳格に必要、§23-3）
    pick: "**Rust**"
    tools: [rust]
    next: Go
    example: 広告入札、取引、リアルタイム対戦の判定
    reason: GCによる停止がない
  - situation: CPU負荷の高い計算
    pick: "**Rust**"
    tools: [rust]
    next: Go
    example: 画像・動画変換、圧縮、暗号、シミュレーション
    reason: ネイティブ性能とメモリ効率
  - situation: 数値計算・機械学習
    pick: "**Python**"
    tools: [python]
    next: Rust（推論のみ）
    example: 推論API、レコメンド、特徴量計算
    reason: ライブラリがPythonに集中している
  - situation: 複雑な業務ルールと厳密な状態管理
    pick: "**Kotlin**"
    tools: [kotlin]
    next: Go、TypeScript
    example: 会計、金融、保険、大規模な在庫・物流
    reason: sealed classなどで状態を型で表現しやすく、トランザクションやバッチの基盤が成熟している
  - situation: 大規模バッチ
    pick: "**Kotlin（Spring Batch）**"
    tools: [kotlin]
    next: Go、Python
    example: 夜間の一括計算、請求締め、データ移行
    reason: 再実行・中断再開・分割実行の仕組みが揃っている
  - situation: ストリーム処理
    pick: "**Kotlin**（Kafka Streams）"
    tools: [kotlin]
    next: Go
    example: Kafkaのイベント集計、リアルタイム分析
    reason: Kafka周辺のエコシステムがJVM中心
  - situation: 長時間ワークフロー
    pick: 言語よりエンジンで決める：**Temporal**（Go／TypeScript）
    tools: [temporal]
    next: Step Functions
    example: 決済→発送→通知、人の承認待ち
    reason: リトライ・待機・状態保持をエンジンに任せる
  - situation: サーバーレスのイベント処理
    pick: "**TypeScript**"
    tools: [typescript]
    next: Go、Python
    example: SQS消費、S3トリガー、定期実行
    reason: 起動が速く、AWS SDKが充実。起動速度を詰めるならGo
  - situation: GraphQLサーバー
    pick: "**TypeScript**"
    tools: [typescript]
    next: Go（gqlgen）
    example: 多様なクライアント向けの集約API
    reason: GraphQLのツール群がTypeScript中心
  - situation: サービス間のRPC
    pick: "**Go**（Connect）"
    tools: [go]
    next: Kotlin、Rust
    example: 社内マイクロサービス
    reason: Protocol Buffers周りのツールが充実し、単一バイナリで配りやすい
```

#### 手順3：チームと変更頻度で最終調整

- 仕様が頻繁に変わる・試作段階なら、手順2の結果より開発速度を優先し、Rails／TypeScriptで始める。Go・Kotlin・Rust・Elixirは本番化の段階で切り替え・切り出しを検討する。
- 担当者が1〜2人なら、手順2で選んだGo・KotlinはTypeScriptに、Elixirはマネージドのリアルタイム基盤（§19-9）に置き換える。保守できる人がいない言語を増やさないためである。Rust（厳格なレイテンシ要件）と、手順1の制約で選んだ言語はそのまま使う。
- 3人以上で長期運用が前提で、性能要件が明確なら手順2の結果に従う。

#### 1つのプロダクトで言語を混ぜるときのルール

- 言語の境界はプロセス（サービス）単位にする。同じプロセス内で混ぜるのは、ネイティブ拡張（napi-rs、PyO3）とWebAssemblyのときだけ。
- サービス間の契約はOpenAPIかProtocol Buffersで定義し、クライアントコードは生成する。
- 新しい言語を1つ増やすと、CI、Lint、依存の更新、監視の計装、担当できる人の確保が必要になる。既定の言語では要件を満たせないか、明らかに大きな差がつく場合にだけ増やす。
- 迷ったら既定の言語で作り、計測してボトルネックになった部分だけを適した言語で切り出す。
