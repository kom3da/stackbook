## 3. フロントエンド

確認：2026年9月

```choices
rows:
  - role: アプリ（SSR・フルスタック）
    default: "**Next.js（App Router）**"
    tools: [next-js]
    alts:
      - tools: [tanstack-start]
        when: Vercel依存を避けたい、クライアント主体で型安全なルーティングを重視するとき
      - name: React Router（フレームワークモード）
        tools: [react-router]
        when: Remix系の資産があるとき
  - role: SPA（ログイン後の管理画面など）
    default: "**React＋Vite＋TanStack Router**"
    tools: [react, vite, tanstack-router]
  - role: 静的サイト・コンテンツ中心
    default: "**Astro**"
    tools: [astro]
    alts:
      - name: Next.js（SSG）
        tools: [next-js]
        when: 同じリポジトリでアプリ部分も持つとき
  - role: CSS
    default: "**Tailwind CSS**"
    tools: [tailwind-css]
    alts:
      - tools: [css-modules]
        when: デザイナーが素のCSSを書くチーム
  - role: UIコンポーネント
    default: "**shadcn/ui**（コードをコピーして所有する方式、Radix UIベース）"
    tools: [shadcn-ui]
    alts:
      - tools: [mantine]
        when: コンポーネントを一式そのまま使いたい管理画面
      - tools: [mui]
        when: Material Design指定のとき
  - role: アイコン
    default: "**lucide-react**"
    tools: [lucide-react]
  - role: サーバー状態（API取得・キャッシュ）
    default: "**TanStack Query**"
    tools: [tanstack-query]
    alts:
      - tools: [swr]
        when: Vercel系の小規模案件
  - role: クライアント状態
    default: "**useState／Context（まず標準で済ませる）→ 必要ならZustand**"
    tools: [usestate, context, zustand]
    alts:
      - tools: [jotai]
        when: 細かい粒度の状態が多いとき
  - role: フォーム
    default: "**React Hook Form＋Zod**"
    tools: [react-hook-form, zod]
    alts:
      - tools: [conform]
        when: Server Actions中心で書くとき
      - tools: [tanstack-form]
        when: 型安全を最優先するとき
  - role: テーブル
    default: "**TanStack Table**"
    tools: [tanstack-table]
    alts:
      - tools: [ag-grid]
        when: 表計算並みの編集機能が必要な業務画面
  - role: グラフ
    default: "**Recharts**"
    tools: [recharts]
    alts:
      - tools: [apache-echarts]
        when: データ量が多い、または地図・複雑なチャートが必要なとき
  - role: リッチテキストエディタ
    default: "**Tiptap**"
    tools: [tiptap]
    alts:
      - tools: [lexical]
        when: Meta製でカスタマイズを深くしたいとき
  - role: 日付選択
    default: "**react-day-picker**（shadcn/uiのCalendarが内部で使用）"
    tools: [react-day-picker]
  - role: アニメーション
    default: "**Motion**（旧Framer Motion）"
    tools: [motion]
    alts:
      - name: CSSのみ
        when: 簡単なトランジションなら不要
  - role: 国際化
    default: "**next-intl**（Next.js）"
    tools: [next-intl]
    alts:
      - name: i18next（react-i18next）
        tools: [i18next, react-i18next]
        when: Next.js以外
  - role: ドラッグ＆ドロップ
    default: "**dnd kit**"
    tools: [dnd-kit]
  - role: 仮想スクロール
    default: "**TanStack Virtual**"
    tools: [tanstack-virtual]
  - role: トースト
    default: "**Sonner**"
    tools: [sonner]
  - role: コンポーネントカタログ
    default: "**Storybook**"
    tools: [storybook]
    alts:
      - tools: [ladle]
        when: 軽く済ませたいとき
  - role: アクセシビリティ検査
    default: "**axe（@axe-core/playwright でE2Eに組み込む）**"
    tools: [axe]
  - role: 画像最適化
    default: "**next/image**（Next.js）／ **astro:assets**（Astro）"
    tools: [next-image, astro-assets]
  - role: SEO・OGP
    default: "**フレームワーク標準のメタデータAPI**"
```

### モバイルアプリ

```choices
rows:
  - role: クロスプラットフォーム
    default: "**React Native＋Expo**"
    tools: [react-native, expo]
    alts:
      - tools: [flutter]
        when: デザインの完全な一貫性を重視し、Dartを受け入れられるとき
      - tools: [hotwire-native]
        when: Railsで作っていて画面の多くがWebで済むとき
  - role: ビルド・配信
    default: "**EAS Build／EAS Submit**"
    tools: [eas-build, eas-submit]
  - role: OTA更新
    default: "**EAS Update**"
    tools: [eas-update]
  - role: ナビゲーション
    default: "**Expo Router**"
    tools: [expo-router]
  - role: スタイル
    default: "**NativeWind（Tailwind）**"
    tools: [nativewind]
```
