# AGENTS.md

このリポジトリで作業する AI への指示。**ここには、どのツールでも必ず守ることだけ**を書く。本文とデータの書き方、画面の確認、Issue とコミットの決まりは `CLAUDE.md` にあるので、作業の前に読むこと。

1. **正本は `content/`（本文の Markdown と `tools.yaml`）。** HTML や AI 向け Markdown はビルドで作るので、手で直さない
2. **事実は一次情報で確かめる。** 記憶で書かない。バージョン番号と料金の金額は書かない
3. **各カテゴリの「既定」は1つだけ。** 代替には「乗り換える条件」を書く。PHP は選択肢に入れない
4. **セクション番号（§N、§N-M）を変えたら、参照をすべて直す。** テスト（`pnpm test`）が参照切れを検出する
5. **秘密情報をコミットしない。** gitleaks がコミット前に検査する
6. 変更の後は `pnpm exec biome ci . && pnpm exec astro check && pnpm test && pnpm build` を通す
