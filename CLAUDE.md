# stackbook

ケース別の技術スタック・周辺ツール選定ガイド。Markdownを正本とし、Astroで静的サイト（HTMLとAI向けMarkdown）に変換する。セットアップ・コマンド・構成は README.md を参照。

## 内容のルール

- PHPは選択肢に入れない
- 各カテゴリの「既定」は1つだけ。代替には必ず「乗り換える条件」を書く
- バージョン番号と料金の金額は書かない
- セクション番号（§N、§N-M）は相互参照とURL（`/s/N/#N-M`）に使っている。番号を変えたら本文の参照、`src/lib/guide.ts` の `SECS`、`src/lib/wizard.ts` の参照も直す
- 「作る」の判定ロジック（`src/lib/wizard.ts`）は §2-9・§2-10・§19 と一致させる。本文を変えたら判定ロジックと `src/lib/lib.test.ts` も確認する
- 各セクションの見出しの直後に `確認：YYYY年M月` を置く。セクションの内容を見直したら更新する
- 文体は「である」調。事実と目安を区別し、目安には「目安」と明記する

## Markdownの書き方（パーサーが前提にしている形）

- `## N. 見出し` がセクション（id は `N`。「使い方」は `intro`、「追記欄」は `memo`）。`### N-M. 見出し` がサブセクション（アンカーは `N-M`）
- 「役割｜既定｜代替と乗り換え条件」の表（2列目の見出しが「既定」で始まる）は辞書の索引になる
  - 既定の列：ツール名を `**太字**` にする。太字の部分がツール名として扱われる
  - 代替の列：`名前：条件` を `。` で区切って並べる（例：`Bun：〜のとき。Deno：〜のとき`）
- 採用の根拠（§22-2）の表、移行パス（§24）・費用（§25）の表、習熟度表（§27、`名前 / 別名`）は、1列目の名前で辞書と結び付く
- コマンド集（§26）は、見出しに `（§19-N）` を書くと、そのケースの「作る」画面に表示される
- ` ```mermaid ` は構成図としてブラウザ側で描画する。`- [ ]` はチェックリストになる
