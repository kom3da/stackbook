# stackbook

ケース別の技術スタック・周辺ツール選定ガイド。Markdownを正本とし、Astroで静的サイト（HTMLとAI向けMarkdown）に変換する。セットアップ・コマンド・構成は README.md を参照。

## 内容のルール

- PHPは選択肢に入れない
- 各カテゴリの「既定」は1つだけ。代替には必ず「乗り換える条件」を書く
- バージョン番号と料金の金額は書かない
- セクション番号（§N、§N-M）は相互参照とURL（`/s/N/#N-M`）に使っている。番号を変えたら本文の参照、`src/lib/guide.ts` の `SECS`、`src/lib/wizard.ts` の参照も直す。「§N-M 手順K」は `### N-M.` の下の `#### 手順K` の見出しに飛ぶ（見出しがないとテストが失敗する）
- 「作る」の判定ロジック（`src/lib/wizard.ts`）は構成の文言を持たない。§19 の `stack` 表を土台に、条件に応じて行を本文の `choices`（`§N-M` と役割名）で差し替える。役割名や §19 のレイヤー名を変えたら、テストが参照切れを検出する
- 判定の分岐（どの条件でどの言語か）は §2-9・§2-10 と一致させる。画面に出す選択肢は、どれも結果を変えるものだけにする（テストで検査している）
- 各セクションの見出しの直後に `確認：YYYY年M月` を置く。セクションの内容を見直したら更新する
- 用語：仕事として受ける単位は「案件」、リポジトリや雛形など技術的な単位は「プロジェクト」。代替へ移ることは「乗り換える」で統一する
- 文体は「である」調。事実と目安を区別し、目安には「目安」と明記する

## 本文とデータの書き方

- 本文は `content/guide/NN.md`（`00-intro.md`、`01.md`〜`27.md`、`99-memo.md`）。ファイル名順に連結して1つのガイドになる
- `## N. 見出し` がセクション（id は `N`）、`### N-M. 見出し` がサブセクション（アンカーは `N-M`）。見出しの直後に `確認：YYYY年M月`
- 辞書・判定に使う表は、本文中の YAML ブロックで書く。種類とフィールドは `src/lib/schema.ts` の `DATA` が正本で、未知のキーや不足はビルドが失敗する
  - ```` ```choices ````：役割｜既定｜代替と乗り換え条件（`role` `default` `tools` `alts[{name?, tools, when?}]` `note?`）
  - ```` ```uses ````（§2-10）、```` ```stack ````（§19）、```` ```rationale ````（§22-2）、```` ```growth ````（§24）、```` ```cost ````（§25）、```` ```prof ````（§27）
- 「作る」画面に出るツールには `ops`（`code`＝自分で書く、`self`＝自分で運用する、`managed`＝マネージドに任せる）を付ける。「作る」画面のツール名の前の記号（■／□／点線の○）、構成図のノードの形、AI 向けの「運用の内訳」に使う。付け忘れはテストが検出する
- ツールは `content/tools.yaml` に `id: 表示名`（言語は `{name, lang: 2-N}`）で登録し、YAML ブロックと `src/lib/wizard.ts` からは id で参照する。未登録の id はビルドが失敗する
- 上記以外の表は通常の Markdown の表で書いてよい（リンクや辞書には使われない）
- コマンド集（§26）は、見出しに `（§19-N）` を書くと、そのケースの「作る」画面に表示される
- ` ```mermaid ` は構成図。`pnpm diagrams` で `src/diagrams/<hash>.svg` に変換してコミットする（コミット前のフックが自動で実行する。未変換だとビルドが止まる）。`- [ ]` はチェックリストになる
